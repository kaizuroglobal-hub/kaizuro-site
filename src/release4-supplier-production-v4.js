import app,{PartnerReferrals}from"./founder-order-communications-v4.js";
export{PartnerReferrals};

const ROOT="/kaizuro-admin",STORE="kaizuro-partner-submissions";
const PRODUCTS=["ASSAULT PE6-8","HALO PE10-12"];
const STATUSES=["Prototype Approved","Production Confirmed","In Production","QC","Packaging","Ready to Ship","Shipped"];
const text=(v,max=1000)=>String(v??"").trim().slice(0,max);
const low=v=>text(v).toLowerCase();
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const rid=p=>`${p}-${crypto.randomUUID().replace(/-/g,"").slice(0,12).toUpperCase()}`;
function db(env){return env.PARTNER_REFERRALS.get(env.PARTNER_REFERRALS.idFromName(STORE));}
async function all(env,t){try{return await db(env).listAll(t)}catch{return[]}}
async function save(env,row){return db(env).createSubmission({...row,createdAt:row.createdAt||new Date().toISOString()});}
function validOrigin(r){const o=r.headers.get("Origin");return !o||o===new URL(r.url).origin;}
async function authed(r,env,ctx){const u=new URL(r.url);u.pathname=`${ROOT}/orders`;u.search="";return (await app.fetch(new Request(u,{method:"GET",headers:r.headers}),env,ctx)).status===200;}
function latestBy(rows,keyFn){const m=new Map();for(const r of rows){const k=keyFn(r),prev=m.get(k);if(!prev||String(r.createdAt||"")>String(prev.createdAt||""))m.set(k,r);}return m;}
function num(v){const n=Number(v);return Number.isFinite(n)&&n>=0?n:0;}
function dateLabel(v){if(!v)return"—";const d=new Date(`${v}T00:00:00Z`);return Number.isNaN(d.getTime())?text(v,40):new Intl.DateTimeFormat("en-AU",{day:"2-digit",month:"short",year:"numeric",timeZone:"UTC"}).format(d);}
function allocationCounts(sales){const out={"ASSAULT PE6-8":0,"HALO PE10-12":0};for(const s of sales){const p=text(s.product,120).toUpperCase();if(p.includes("ASSAULT"))out["ASSAULT PE6-8"]++;else if(p.includes("HALO"))out["HALO PE10-12"]++;}return out;}
async function snapshot(env){const [sales,plans]=await Promise.all([all(env,"sale"),all(env,"supplier-production-plan")]);const latest=latestBy(plans,r=>text(r.product,120));const allocated=allocationCounts(sales);return PRODUCTS.map(product=>{const p=latest.get(product)||{};return{product,allocated:allocated[product]||0,productionQty:num(p.productionQty),status:text(p.status,80)||"Prototype Approved",batchRef:text(p.batchRef,120),factoryCompletion:text(p.factoryCompletion,20),dispatchDate:text(p.dispatchDate,20),qcStatus:text(p.qcStatus,80)||"Not started",packagingStatus:text(p.packagingStatus,80)||"Not started",notes:text(p.notes,1500),updatedAt:text(p.createdAt,40)};});}
async function sha256(s){const b=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(s));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,"0")).join("");}
async function activeShare(env){const rows=await all(env,"supplier-production-share");return rows.sort((a,b)=>String(b.createdAt||"").localeCompare(String(a.createdAt||"")))[0]||null;}
async function tokenValid(env,token){if(!token)return false;const a=await activeShare(env);if(!a?.tokenHash)return false;return (await sha256(token))===a.tokenHash;}
function adminCss(){return `body{margin:0;background:#efefec;color:#111;font-family:Inter,Arial,sans-serif}.wrap{max-width:1180px;margin:48px auto;padding:0 22px}.top{display:flex;justify-content:space-between;gap:20px;align-items:end;margin-bottom:24px}.eyebrow{font-size:11px;letter-spacing:.14em;text-transform:uppercase;color:#777}.card{background:#fff;border:1px solid #d5d6d2;padding:22px;margin-bottom:18px}h1{margin:5px 0 0;font-size:34px}h2{margin:0 0 14px}.grid{display:grid;grid-template-columns:1.25fr .65fr .8fr .85fr .85fr .9fr .9fr;gap:10px;align-items:end}.field label{display:block;font-size:10px;letter-spacing:.08em;text-transform:uppercase;color:#777;margin-bottom:6px}.field input,.field select,.field textarea{width:100%;box-sizing:border-box;border:1px solid #cfd0cc;background:#fafafa;padding:10px;font:13px Arial}.field textarea{min-height:78px}.wide{grid-column:1/-1}.btn{display:inline-block;border:1px solid #111;background:#111;color:#fff;padding:11px 14px;font:700 11px Arial;text-transform:uppercase;letter-spacing:.04em;text-decoration:none;cursor:pointer}.btn.light{background:#fff;color:#111}.actions{display:flex;gap:9px;flex-wrap:wrap}.metric{font-size:28px;font-weight:800}.muted{color:#777;font-size:12px}.sharebox{display:flex;gap:8px;margin-top:12px}.sharebox input{flex:1;padding:11px;border:1px solid #ccc;background:#f8f8f6}@media(max-width:900px){.grid{grid-template-columns:1fr 1fr}.wide{grid-column:1/-1}}`;}
function statusOptions(current){return STATUSES.map(s=>`<option${s===current?" selected":""}>${esc(s)}</option>`).join("");}
async function adminPage(request,env,ctx){if(!(await authed(request,env,ctx)))return new Response("Unauthorized",{status:403});const rows=await snapshot(env),share=await activeShare(env),u=new URL(request.url),msg=u.searchParams.get("saved")?"Production plan saved.":u.searchParams.get("shared")?"New supplier link created. Previous supplier links are now invalid.":"";return new Response(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Supplier Production · KAIZURO</title><style>${adminCss()}</style></head><body><main class="wrap"><div class="top"><div><div class="eyebrow">KAIZURO · Release 4</div><h1>Supplier Production</h1><p class="muted">One controlled production dataset feeding the private supplier link, PDF and CSV.</p></div><a class="btn light" href="${ROOT}/orders">Back to Orders</a></div>${msg?`<div class="card"><b>${esc(msg)}</b></div>`:""}<div class="card"><h2>Founder allocation snapshot</h2><div class="actions">${rows.map(r=>`<div><span class="muted">${esc(r.product)}</span><div class="metric">${r.allocated}</div><span class="muted">Founder allocations</span></div>`).join("")}</div></div>${rows.map((r,i)=>`<form class="card" method="post" action="${ROOT}/supplier-production/save"><input type="hidden" name="product" value="${esc(r.product)}"><h2>${esc(r.product)}</h2><div class="grid"><div class="field"><label>Batch / PO reference</label><input name="batchRef" value="${esc(r.batchRef)}" placeholder="e.g. PO-001"></div><div class="field"><label>Production qty</label><input type="number" min="0" name="productionQty" value="${r.productionQty||""}"></div><div class="field"><label>Status</label><select name="status">${statusOptions(r.status)}</select></div><div class="field"><label>Factory completion</label><input type="date" name="factoryCompletion" value="${esc(r.factoryCompletion)}"></div><div class="field"><label>Dispatch date</label><input type="date" name="dispatchDate" value="${esc(r.dispatchDate)}"></div><div class="field"><label>QC status</label><input name="qcStatus" value="${esc(r.qcStatus)}"></div><div class="field"><label>Packaging status</label><input name="packagingStatus" value="${esc(r.packagingStatus)}"></div><div class="field wide"><label>Supplier notes</label><textarea name="notes" placeholder="Only information safe to share with the supplier">${esc(r.notes)}</textarea></div></div><div class="actions" style="margin-top:14px"><button class="btn" type="submit">Save ${esc(r.product)}</button><span class="muted">Allocated now: ${r.allocated}${r.updatedAt?` · Last saved ${esc(new Date(r.updatedAt).toLocaleString("en-AU",{timeZone:"Australia/Sydney"}))}`:""}</span></div></form>`).join("")}<div class="card"><h2>Supplier sharing</h2><p class="muted">The supplier page contains production information only. Customer identity, contact details, payment data and Square references are excluded.</p><form method="post" action="${ROOT}/supplier-production/share"><button class="btn" type="submit">${share?"Regenerate Private Supplier Link":"Create Private Supplier Link"}</button></form>${share?`<p class="muted">An active private supplier link exists. Regenerating it invalidates the previous link.</p>`:""}</div></main></body></html>`,{headers:{"Content-Type":"text/html; charset=UTF-8","Cache-Control":"no-store","X-Robots-Tag":"noindex,nofollow,noarchive"}});}
async function savePlan(request,env,ctx){if(!validOrigin(request)||!(await authed(request,env,ctx)))return new Response("Unauthorized",{status:403});const f=await request.formData(),product=text(f.get("product"),120);if(!PRODUCTS.includes(product))return new Response("Unsupported product",{status:400});const status=text(f.get("status"),80);if(!STATUSES.includes(status))return new Response("Invalid production status",{status:400});await save(env,{id:rid("KZPROD"),type:"supplier-production-plan",partnerId:"network",product,batchRef:text(f.get("batchRef"),120),productionQty:num(f.get("productionQty")),status,factoryCompletion:text(f.get("factoryCompletion"),20),dispatchDate:text(f.get("dispatchDate"),20),qcStatus:text(f.get("qcStatus"),80),packagingStatus:text(f.get("packagingStatus"),80),notes:text(f.get("notes"),1500),actor:"KAIZURO Admin"});return Response.redirect(new URL(`${ROOT}/supplier-production?saved=1`,request.url),303);}
function randomToken(){const a=new Uint8Array(32);crypto.getRandomValues(a);return [...a].map(x=>x.toString(16).padStart(2,"0")).join("");}
async function createShare(request,env,ctx){if(!validOrigin(request)||!(await authed(request,env,ctx)))return new Response("Unauthorized",{status:403});const token=randomToken(),tokenHash=await sha256(token),id=rid("KZSHARE");await save(env,{id,type:"supplier-production-share",partnerId:"network",tokenHash,actor:"KAIZURO Admin"});const base=new URL(request.url).origin,link=`${base}/supplier/production?token=${token}`;return new Response(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Supplier link created</title><style>${adminCss()}</style></head><body><main class="wrap"><div class="card"><div class="eyebrow">KAIZURO · Release 4</div><h1>Private supplier link created</h1><p>This is the only active supplier link. Copy it now. KAIZURO stores only its hash, not the original token.</p><div class="sharebox"><input id="share" readonly value="${esc(link)}"><button class="btn" onclick="navigator.clipboard.writeText(document.getElementById('share').value);this.textContent='Copied'">Copy Link</button></div><div class="actions" style="margin-top:18px"><a class="btn light" href="${esc(link)}" target="_blank" rel="noopener">Open Supplier View</a><a class="btn light" href="${ROOT}/supplier-production">Back</a></div></div></main></body></html>`,{headers:{"Content-Type":"text/html; charset=UTF-8","Cache-Control":"no-store","X-Robots-Tag":"noindex,nofollow,noarchive"}});}
function supplierCss(){return `body{margin:0;background:#f0f0ed;color:#111;font-family:Arial,sans-serif}.wrap{max-width:1080px;margin:44px auto;padding:0 20px}.hero{background:#111;color:#fff;padding:28px}.brand{font-weight:900;letter-spacing:.16em}.sub{color:#aaa;font-size:11px;letter-spacing:.12em;text-transform:uppercase;margin-top:5px}.toolbar{display:flex;gap:9px;margin:16px 0;flex-wrap:wrap}.btn{border:1px solid #111;background:#111;color:#fff;padding:10px 13px;text-decoration:none;font-size:11px;font-weight:700;text-transform:uppercase}.card{background:#fff;border:1px solid #d4d5d1;margin:14px 0;padding:20px}.row{display:grid;grid-template-columns:1.35fr .75fr .8fr .9fr .9fr;gap:14px;padding:12px 0;border-top:1px solid #e4e4e0}.row:first-of-type{border-top:0}.lab{font-size:10px;text-transform:uppercase;color:#777;letter-spacing:.07em}.val{font-weight:700;margin-top:5px}.notes{white-space:pre-wrap;line-height:1.5}.privacy{font-size:11px;color:#777;margin-top:22px}@media(max-width:700px){.row{grid-template-columns:1fr 1fr}}@media print{body{background:#fff}.toolbar{display:none}.wrap{margin:0;max-width:none}.card,.hero{break-inside:avoid}}`;}
function supplierHtml(rows,token){const q=encodeURIComponent(token);return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>KAIZURO Supplier Production</title><style>${supplierCss()}</style></head><body><main class="wrap"><section class="hero"><div class="brand">KAIZURO</div><div class="sub">Supplier Production Report</div></section><div class="toolbar"><a class="btn" href="/supplier/production.pdf?token=${q}">Download PDF</a><a class="btn" href="/supplier/production.csv?token=${q}">Download CSV</a><a class="btn" href="#" onclick="window.print();return false">Print</a></div><section class="card"><div class="lab">Live Founder Allocation</div><p>This report shows KAIZURO production requirements and supplier-facing milestones. It updates from the KAIZURO production system.</p></section>${rows.map(r=>`<section class="card"><h2>${esc(r.product)}</h2><div class="row"><div><div class="lab">Batch / PO</div><div class="val">${esc(r.batchRef||"—")}</div></div><div><div class="lab">Founder allocated</div><div class="val">${r.allocated}</div></div><div><div class="lab">Production qty</div><div class="val">${r.productionQty||"—"}</div></div><div><div class="lab">Status</div><div class="val">${esc(r.status)}</div></div><div><div class="lab">Factory completion</div><div class="val">${esc(dateLabel(r.factoryCompletion))}</div></div></div><div class="row"><div><div class="lab">Dispatch date</div><div class="val">${esc(dateLabel(r.dispatchDate))}</div></div><div><div class="lab">QC</div><div class="val">${esc(r.qcStatus)}</div></div><div><div class="lab">Packaging</div><div class="val">${esc(r.packagingStatus)}</div></div><div style="grid-column:span 2"><div class="lab">KAIZURO notes</div><div class="val notes">${esc(r.notes||"—")}</div></div></div></section>`).join("")}<div class="privacy">Supplier-confidential production information. Customer identity and payment information are intentionally excluded.</div></main></body></html>`;}
function csvCell(v){const s=String(v??"");return `"${s.replace(/"/g,'""')}"`;}
function csv(rows){const head=["Product","Batch / PO","Founder Allocated","Production Qty","Status","Factory Completion","Dispatch Date","QC Status","Packaging Status","KAIZURO Notes"];return [head,...rows.map(r=>[r.product,r.batchRef,r.allocated,r.productionQty,r.status,r.factoryCompletion,r.dispatchDate,r.qcStatus,r.packagingStatus,r.notes])].map(row=>row.map(csvCell).join(",")).join("\r\n");}
function pdfEscape(s){return String(s??"").replace(/\\/g,"\\\\").replace(/\(/g,"\\(").replace(/\)/g,"\\)").replace(/[\r\n]+/g," ");}
function pdfBytes(rows){const lines=["KAIZURO - SUPPLIER PRODUCTION REPORT","","Supplier-confidential production information","",...rows.flatMap(r=>[r.product,`Batch / PO: ${r.batchRef||"-"}`,`Founder allocated: ${r.allocated}    Production qty: ${r.productionQty||"-"}`,`Status: ${r.status}`,`Factory completion: ${dateLabel(r.factoryCompletion)}    Dispatch: ${dateLabel(r.dispatchDate)}`,`QC: ${r.qcStatus}    Packaging: ${r.packagingStatus}`,`Notes: ${r.notes||"-"}`,""] )];let y=800,content="BT /F1 11 Tf 50 820 Td ";for(const line of lines){content+=`(${pdfEscape(line).slice(0,115)}) Tj 0 -16 Td `;y-=16;if(y<40)break;}content+="ET";const objs=[];objs[1]="<< /Type /Catalog /Pages 2 0 R >>";objs[2]="<< /Type /Pages /Kids [3 0 R] /Count 1 >>";objs[3]="<< /Type /Page /Parent 2 0 R /MediaBox [0 0 595 842] /Resources << /Font << /F1 5 0 R >> >> /Contents 4 0 R >>";objs[4]=`<< /Length ${new TextEncoder().encode(content).length} >>\nstream\n${content}\nendstream`;objs[5]="<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica >>";let out="%PDF-1.4\n",offsets=[0];for(let i=1;i<=5;i++){offsets[i]=new TextEncoder().encode(out).length;out+=`${i} 0 obj\n${objs[i]}\nendobj\n`;}const xref=new TextEncoder().encode(out).length;out+="xref\n0 6\n0000000000 65535 f \n";for(let i=1;i<=5;i++)out+=`${String(offsets[i]).padStart(10,"0")} 00000 n \n`;out+=`trailer\n<< /Size 6 /Root 1 0 R >>\nstartxref\n${xref}\n%%EOF`;return new TextEncoder().encode(out);}
async function supplierRoute(request,env,format){const u=new URL(request.url),token=text(u.searchParams.get("token"),200);if(!(await tokenValid(env,token)))return new Response("This supplier link is invalid or has been replaced.",{status:403,headers:{"Cache-Control":"no-store","X-Robots-Tag":"noindex,nofollow,noarchive"}});const rows=await snapshot(env);if(format==="csv")return new Response(csv(rows),{headers:{"Content-Type":"text/csv; charset=UTF-8","Content-Disposition":"attachment; filename=KAIZURO-supplier-production.csv","Cache-Control":"no-store","X-Robots-Tag":"noindex,nofollow,noarchive"}});if(format==="pdf")return new Response(pdfBytes(rows),{headers:{"Content-Type":"application/pdf","Content-Disposition":"attachment; filename=KAIZURO-supplier-production.pdf","Cache-Control":"no-store","X-Robots-Tag":"noindex,nofollow,noarchive"}});return new Response(supplierHtml(rows,token),{headers:{"Content-Type":"text/html; charset=UTF-8","Cache-Control":"no-store","X-Robots-Tag":"noindex,nofollow,noarchive","Referrer-Policy":"no-referrer"}});}
export default{async fetch(request,env,ctx){const u=new URL(request.url),path=u.pathname.replace(/\/$/,"")||"/";if(request.method==="GET"&&path===`${ROOT}/supplier-production`)return adminPage(request,env,ctx);if(request.method==="POST"&&path===`${ROOT}/supplier-production/save`)return savePlan(request,env,ctx);if(request.method==="POST"&&path===`${ROOT}/supplier-production/share`)return createShare(request,env,ctx);if(request.method==="GET"&&path==="/supplier/production")return supplierRoute(request,env,"html");if(request.method==="GET"&&path==="/supplier/production.csv")return supplierRoute(request,env,"csv");if(request.method==="GET"&&path==="/supplier/production.pdf")return supplierRoute(request,env,"pdf");return app.fetch(request,env,ctx);},async email(message,env,ctx){if(typeof app.email==="function")return app.email(message,env,ctx)}};